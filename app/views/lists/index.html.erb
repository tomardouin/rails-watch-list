<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://img.freepik.com/photos-gratuite/vue-salle-cinema-3d_23-2151067056.jpg);">
  <div class="container">
    <h1>Ajouter some movies to different kind of list</h1>
    <p>Find the movies you want to watch depending of your mood</p>
  </div>
</div>

<div class='container pt-5'>
    <h1 class='text-center'> All of the list </h1>
    <ul class='align-items-center'>
        <% @lists.each do |list| %>
        <li>
            <h2> <%= link_to list.name, list_path(list) %> | <%= link_to "Delete", list_path(list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> </h2>
        </li>
        <% end %>
    </ul>
    <%= link_to "Create a new list", new_list_path, class:'btn btn-primary' %>
</div>